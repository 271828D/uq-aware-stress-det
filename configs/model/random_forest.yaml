# @package _global_
defaults:
  - _self_

model:
  model_name: random_forest
  n_estimators: 100
  max_depth: null
  min_samples_split: 2  
  random_state: ${seed} # Ensure seed is passed here too if needed   